#include <stdio.h>

int main(void) {
   char userName[50];
   
   scanf("%s", userName);
   printf("Hello %s, and welcome to CS Online!\n", userName);
   /* Hello Pat, and welcome to CS Online! */

   return 0;
}